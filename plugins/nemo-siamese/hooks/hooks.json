{
  "description": "SessionStart greeting — prints the NEMO banner (cat + ball) followed by a random cat fact.",
  "hooks": {
    "SessionStart": [
      {
        "hooks": [
          {
            "type": "command",
            "command": "bash \"${CLAUDE_PLUGIN_ROOT}/hooks/greet.sh\""
          }
        ]
      }
    ]
  }
}
